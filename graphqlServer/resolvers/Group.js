const Group = {
  // async owner(parent, args, { prisma }, info) {
  //   return await prisma.group({ id: parent.id }).owner();
  // },
  // async cards(parent, args, { prisma }, info) {
  //   return await prisma.group({ id: parent.id }).cards();
  // }
};

module.exports = Group;